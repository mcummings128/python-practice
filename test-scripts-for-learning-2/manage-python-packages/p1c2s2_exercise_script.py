# This started off with ModuleNotFound errors for boto3 and flask. Using python3 -m pip install worked to resolve these issues, then the print statement was seen.

import boto3
import numpy
import requests
from flask import Flask


print("Congratulations. You have installed the right packages and your script has executed successfully!")